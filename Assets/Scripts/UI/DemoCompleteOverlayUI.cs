using UnityEngine;

namespace ETD.UI
{
    /// <summary>
    /// Deprecated compatibility shim.
    /// The old version auto-created a fallback overlay at runtime. That behavior is removed.
    /// Use DemoUI on your own hand-built DemoUI panel instead.
    /// </summary>
    [System.Obsolete("Use DemoUI on your own Demo Complete panel instead. This script intentionally does nothing.")]
    public sealed class DemoCompleteOverlayUI : MonoBehaviour
    {
    }
}
